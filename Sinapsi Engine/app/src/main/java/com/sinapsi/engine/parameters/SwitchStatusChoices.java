package com.sinapsi.engine.parameters;

/**
* Enum of possible values that a parameter of type "choice"
* can have in a enabled/disabled meaning
*/
public enum SwitchStatusChoices {
    ENABLING,
    ENABLED,
    DISABLING,
    DISABLED
}
