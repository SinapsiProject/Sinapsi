package com.sinapsi.engine.parameters;

/**
* Enum of possible values that a parameter of type "choice"
* can have in a connection meaning
*/
public enum ConnectionStatusChoices {
    CONNECTING,
    CONNECTED,
    DISCONNECTING,
    DISCONNECTED
}
