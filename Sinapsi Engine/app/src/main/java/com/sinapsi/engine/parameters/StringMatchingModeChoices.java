package com.sinapsi.engine.parameters;

/**
 * Enum of possible values that a parameter of type "advanced_string"
 * can have in a string matching meaning.
 */
public enum StringMatchingModeChoices {
    WHOLE_STRING,
    CONTAINS,
    REGEX
}
